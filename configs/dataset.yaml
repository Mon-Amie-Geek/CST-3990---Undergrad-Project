# configs/dataset.yaml
# Created as early preparation - To be actively used for YOLOv8n training

# NOTE: Colab-only path — used exclusively during YOLOv8 training on Google Colab
path: /content/drive/MyDrive/CST3990/data/ua_detrac/processed
train: images/train
val: images/val
test: images/test

nc: 1
names: ['Vehicle']

# Training resolution : original 960x540 - No manual resize
# Inference at 640x640 - Declared in config_blockA.yaml
# Occlusion filter [0,1] - occ 2/3 excluded during ingestion.
# File is required for YOLO Training
# seed=42 passed to model.train() separately
# conf=0.25 , nms=0.45 - Identical across both detectors
# <class_id> <cx> <cy> <w> <h> : All values normalised 0-1


